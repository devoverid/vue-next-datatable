import VueNextDatatable from 'vue-next-datatable'
import { mount } from '@vue/test-utils'

describe('VueNextDatatable', () => {
  it('works', () => {
    expect(VueNextDatatable).toBeTruthy()
  })
})
